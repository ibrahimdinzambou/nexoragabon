[build]
builder = "RAILPACK"
buildCommand = "./mvnw -DskipTests package"

[deploy]
startCommand = "java -jar target/iptv-saas-api-0.1.0.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
