diff --git a/src/lib/puppeteer.ts b/src/lib/puppeteer.ts
index 38e21b1..04b3d68 100644
--- a/src/lib/puppeteer.ts
+++ b/src/lib/puppeteer.ts
@@ -4,6 +4,7 @@ let browser: Browser | null = null;
 
 const BAD_URL_RX = /(doubleclick|googlesyndication|googletagmanager|google-analytics|facebook|fbcdn|disqus|popads|popcash|exoclick|adnxs|mgid|taboola|outbrain|adsterra|hilltopads|trafficjunky|propellerads|analytics|tracking|ads|advertisement)/i;
 const NEXORA_NAVIGATION_HOSTS = ['nexoragabon.com', 'www.nexoragabon.com'] as const;
+const PLAYER_PROVIDER_RX = /(vidzy|uqload|netu|voe|dood|streamtape|filemoon|lulustream|kokoflix|mixdrop|vidoza|upstream|waaw|streamwish|streamsb|filelions|savefiles|wolfstream|embed|player)/i;
 
 function normalizeHost(value: string): string {
   return String(value || '').trim().toLowerCase().replace(/^https?:\/\//, '').replace(/\/.*$/, '');
@@ -217,6 +218,10 @@ export function attachCollector(page: Page) {
   };
 
   const isLikelyM3U8 = (url: string) => url && /\.m3u8(\?|$)/i.test(url);
+  const isLikelyPlayerEmbed = (url: string) => {
+    if (!url || !/^https?:\/\//i.test(url)) return false;
+    return PLAYER_PROVIDER_RX.test(url) && /(embed|player|stream|video|\/e\/|\/v\/|\/f\/|\/d\/)/i.test(url);
+  };
 
   page.on('request', (req) => {
     try {
@@ -224,7 +229,7 @@ export function attachCollector(page: Page) {
       if (isLikelyM3U8(u)) {
         store.m3u8s.add(u);
       }
-      if (/vidzy\.live\/embed-[a-z0-9]+\.html/i.test(u)) {
+      if (isLikelyPlayerEmbed(u)) {
         store.embeds.add(u);
       }
     } catch {}
@@ -248,13 +253,18 @@ export function attachCollector(page: Page) {
 
       const text = await res.text().catch(() => '');
       if (!text || text.length > 800000) return;
-      if (!/(m3u8|vidzy|source|file)/i.test(text)) return;
+      if (!/(m3u8|source|file|embed|player|stream|vidzy|uqload|netu|voe|dood|streamtape|filemoon|lulustream|kokoflix)/i.test(text)) return;
 
       const m3u8 = extractM3U8FromText(text);
       if (m3u8) store.m3u8s.add(m3u8);
 
       const allUrls = text.match(/https?:\/\/[^\s"'<>\\]+\.m3u8[^\s"'<>\\]*/gi);
       if (allUrls) allUrls.forEach((u) => store.m3u8s.add(u));
+
+      const embedUrls = text.match(/https?:\/\/[^\s"'<>\\]+/gi);
+      if (embedUrls) {
+        embedUrls.filter(isLikelyPlayerEmbed).forEach((u) => store.embeds.add(u));
+      }
     } catch {}
   });
 
diff --git a/src/lib/scraper-series.ts b/src/lib/scraper-series.ts
index 38d454e..e9d87cc 100644
--- a/src/lib/scraper-series.ts
+++ b/src/lib/scraper-series.ts
@@ -3,6 +3,7 @@ import { CONFIG } from './config';
 
 
 const DEBUG_SCRAPER_LOGS = process.env.DEBUG_SCRAPER_LOGS === 'true';
+const PLAYER_PROVIDER_RX = /(vidzy|uqload|netu|voe|dood|streamtape|filemoon|lulustream|kokoflix|mixdrop|vidoza|upstream|waaw|streamwish|streamsb|filelions|savefiles|wolfstream|embed|player)/i;
 function debugLog(...args: unknown[]) {
   if (DEBUG_SCRAPER_LOGS) console.log(...args);
 }
@@ -145,7 +146,8 @@ async function getIframeEmbed(page: any): Promise<string | null> {
   try {
     return await page.evaluate(() => {
       const frames = Array.from(document.querySelectorAll('iframe'));
-      const vidzyFrame = frames.find((f: any) => f.src && f.src.includes('vidzy'));
+      const playerProviderRx = /(vidzy|uqload|netu|voe|dood|streamtape|filemoon|lulustream|kokoflix|mixdrop|vidoza|upstream|waaw|streamwish|streamsb|filelions|savefiles|wolfstream|embed|player)/i;
+      const vidzyFrame = frames.find((f: any) => f.src && playerProviderRx.test(f.src));
       return vidzyFrame ? (vidzyFrame as any).src : null;
     });
   } catch {
diff --git a/src/lib/scraper.ts b/src/lib/scraper.ts
index 8bc3f82..0d3a821 100644
--- a/src/lib/scraper.ts
+++ b/src/lib/scraper.ts
@@ -4,6 +4,7 @@ import { CONFIG } from './config';
 
 
 const DEBUG_SCRAPER_LOGS = process.env.DEBUG_SCRAPER_LOGS === 'true';
+const PLAYER_PROVIDER_RX = /(vidzy|uqload|netu|voe|dood|streamtape|filemoon|lulustream|kokoflix|mixdrop|vidoza|upstream|waaw|streamwish|streamsb|filelions|savefiles|wolfstream|embed|player)/i;
 function debugLog(...args: unknown[]) {
   if (DEBUG_SCRAPER_LOGS) console.log(...args);
 }
@@ -181,7 +182,7 @@ async function scanDOMEmbeds(page: any, sourceUrl: string): Promise<string[]> {
       return out;
     });
 
-    return (urls || []).filter((u: string) => /vidzy\.live\/embed-[a-z0-9]+\.html/i.test(u));
+    return (urls || []).filter((u: string) => PLAYER_PROVIDER_RX.test(u));
   } catch {
     return [];
   }
