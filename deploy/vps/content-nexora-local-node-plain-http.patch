diff --git a/src/autoflix_api/node_providers.py b/src/autoflix_api/node_providers.py
index 9a372a2..1736e1c 100644
--- a/src/autoflix_api/node_providers.py
+++ b/src/autoflix_api/node_providers.py
@@ -26,15 +26,14 @@ def configured() -> bool:
 
 
 def _request(path: str, params: dict[str, Any] | None = None) -> dict[str, Any]:
     if not configured():
         raise RuntimeError("FRENCH_NEXORA_API_BASE_URL is not configured")
     response = requests.get(
         f"{base_url()}{path}",
         params=params or {},
         headers={"Accept": "application/json"},
-        impersonate="chrome",
         timeout=int(os.getenv("FRENCH_NEXORA_API_TIMEOUT", "65")),
     )
     response.raise_for_status()
     return response.json()
 
